# Unit Converter – Meters to Feet

A simple Python script that converts a distance value from meters to feet using command-line arguments.

## 🧠 How it works

- The user provides the number of meters as a command-line argument.
- The script calculates and prints the equivalent value in feet.
- Conversion formula:  
  **1 meter = 3.2808 feet**

## Usage

```bash
python unit_converter.py --metry 10

Requirements
Python 3.x

No external libraries needed — only built-in argparse.

Author
Mini-project created as part of learning Python and using argparse